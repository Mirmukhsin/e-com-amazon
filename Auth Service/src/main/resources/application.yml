spring:
  application:
    name: auth-service
  profiles:
    active: dev

#RabbitMQ

  rabbitmq:
    host: localhost
    port: 5672
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}