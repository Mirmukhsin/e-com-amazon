spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: 'jdbc:h2:mem:db'
    username: ecom
    password: 123
    driver-class-name: org.h2.Driver
  h2:
    console:
      path: /h2
      enabled: true

server:
  port: 8080
